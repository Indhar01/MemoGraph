{
  "serverInfo": {
    "name": "memograph",
    "version": "0.1.0"
  },
  "authentication": {
    "required": false,
    "schemes": []
  },
  "tools": [
    {
      "name": "search_vault",
      "description": "Search memories using semantic or keyword search with configurable filters",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "description": "Search query" },
          "limit": { "type": "integer", "description": "Max results to return", "default": 10 }
        },
        "required": ["query"]
      }
    },
    {
      "name": "query_with_context",
      "description": "Retrieve relevant context for a question with token compression for LLM prompts",
      "inputSchema": {
        "type": "object",
        "properties": {
          "question": { "type": "string", "description": "The question to answer" }
        },
        "required": ["question"]
      }
    },
    {
      "name": "create_memory",
      "description": "Create a new memory in the vault with title, content, tags, and type",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "content": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } },
          "type": { "type": "string" }
        },
        "required": ["title", "content"]
      }
    },
    {
      "name": "import_document",
      "description": "Import TXT or MD documents into the vault, preserving formatting and metadata",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "description": "Path to the document to import" }
        },
        "required": ["path"]
      }
    },
    {
      "name": "bulk_create",
      "description": "Create multiple memories in a single operation for efficient batch imports",
      "inputSchema": {
        "type": "object",
        "properties": {
          "memories": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "content": { "type": "string" },
                "tags": { "type": "array", "items": { "type": "string" } }
              },
              "required": ["title", "content"]
            }
          }
        },
        "required": ["memories"]
      }
    },
    {
      "name": "list_memories",
      "description": "List memories with optional filters (tags, type, date range)",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tags": { "type": "array", "items": { "type": "string" } },
          "type": { "type": "string" },
          "limit": { "type": "integer", "default": 20 }
        }
      }
    },
    {
      "name": "get_memory",
      "description": "Get full content and metadata of a specific memory by title",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "description": "Title of the memory to retrieve" }
        },
        "required": ["title"]
      }
    },
    {
      "name": "get_vault_info",
      "description": "Get information about the configured vault (path, size, configuration)",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "get_vault_stats",
      "description": "Get vault statistics and analytics (memory count, tags, types, connections)",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "update_memory",
      "description": "Update an existing memory's content, tags, or metadata",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "content": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["title"]
      }
    },
    {
      "name": "delete_memory",
      "description": "Delete a memory from the vault permanently",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "description": "Title of the memory to delete" }
        },
        "required": ["title"]
      }
    },
    {
      "name": "relate_memories",
      "description": "Create a bidirectional graph link between two memories for enhanced traversal",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source": { "type": "string", "description": "Title of the source memory" },
          "target": { "type": "string", "description": "Title of the target memory" }
        },
        "required": ["source", "target"]
      }
    },
    {
      "name": "search_by_graph",
      "description": "Traverse connected memories from a starting point using BFS with configurable depth",
      "inputSchema": {
        "type": "object",
        "properties": {
          "start": { "type": "string", "description": "Title of the starting memory" },
          "depth": { "type": "integer", "default": 2 }
        },
        "required": ["start"]
      }
    },
    {
      "name": "find_path",
      "description": "Find the shortest graph path between two memories using BFS",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source": { "type": "string" },
          "target": { "type": "string" }
        },
        "required": ["source", "target"]
      }
    }
  ],
  "resources": [],
  "prompts": []
}