apiVersion: v1
kind: Service
metadata:
  name: memograph
  namespace: memograph
  labels:
    app.kubernetes.io/name: memograph
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: memograph
    app.kubernetes.io/instance: memograph
