apiVersion: v1
kind: ServiceAccount
metadata:
  name: memograph
  namespace: memograph
  labels:
    app.kubernetes.io/name: memograph
# No tokens auto-mounted — the API does not call back into the cluster.
automountServiceAccountToken: false
