# Core dependencies for memograph
# For installation, prefer: pip install -e .
# Or with all optional dependencies: pip install -e .[all]

# Core requirements
# Pinned with upper bounds to prevent surprise breakage from major-version
# releases. For reproducible installs, generate a lockfile:
#   pip-compile pyproject.toml -o requirements.lock
# (or `uv pip compile pyproject.toml -o requirements.lock` if you prefer uv).
PyYAML>=6.0,<7
requests>=2.31.0,<3
mcp>=1.0.0,<2

# Optional LLM Provider Dependencies (install as needed)
# For OpenAI: pip install -e .[openai]
# openai>=1.0.0
# tiktoken>=0.5.0

# For Anthropic Claude: pip install -e .[anthropic]
# anthropic>=0.39.0

# For Ollama: pip install -e .[ollama]
# ollama>=0.3.0

# For Embeddings: pip install -e .[embeddings]
# sentence-transformers>=2.2.0
# numpy>=1.24.0

# For Integrations: pip install -e .[integrations]
# watchdog>=3.0.0  # File system monitoring (Obsidian)
# python-frontmatter>=1.0.0  # Markdown frontmatter parsing (Obsidian)
# notion-client>=2.0.0  # Notion API client (Notion)

# Development Dependencies
# For development setup: pip install -e .[dev]
# pytest>=7.4.0
# pytest-cov>=4.1.0
# pytest-asyncio>=0.21.0
# ruff>=0.7.0
# mypy>=1.7.0
# pre-commit>=3.5.0
# black>=23.11.0
# isort>=5.12.0
# types-PyYAML>=6.0
# types-requests>=2.31.0
